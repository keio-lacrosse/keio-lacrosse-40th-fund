# 40周年記念寄付金サイト（部内レビュー用の仮公開）

このフォルダは `scripts/build_review.py` が自動生成する配信用ビルドです。直接編集しないでください。
編集するのは `../site/index.html` です。

レビュー段階のため、口座番号を伏せ字にし、検索エンジンに載らない設定（noindex）を入れています。
本番公開時は site/ をそのまま配信する形に切り替えます。
